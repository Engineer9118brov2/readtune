{
  "name": "readtune",
  "version": "0.9.5",
  "private": true,
  "description": "A Chrome extension that suggests reading settings worth trying, then applies them everywhere — articles, PDFs, and web pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "capture-privacy-check": "node scripts/capture-privacy-check.mjs",
    "harness": "node scripts/harness.mjs",
    "piper-smoke": "node scripts/piper-smoke.mjs",
    "test": "npm run check && npm run capture-privacy-check && npm run harness"
  },
  "license": "MIT"
}
